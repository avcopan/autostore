pre-commit:
  parallel: false
  exclude:
    - .pixi/
  commands:
    fmt:
      run: pixi run --frozen fmt

    lint:
      run: pixi run --frozen lint

    types:
      run: pixi run --frozen types

    imports:
      run: pixi run --frozen imports

    test:
      run: pixi run --frozen test
